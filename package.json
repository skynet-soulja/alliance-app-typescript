{
  "name": "alliance-app-typescript",
  "version": "1.0.0",
  "author": "skynet-soulja",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "clean": "rm server.js && rm createPDF.js",
    "develop": "yarn build:app && ts-node-dev server.ts",
    "build:app": "cd app && yarn install && yarn build",
    "build:server": "tsc",
    "build": "yarn build:app && yarn build:server",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.6",
    "@types/nodemailer": "^6.4.0",
    "@types/puppeteer": "^5.4.0",
    "nodemon": "^2.0.6",
    "ts-node-dev": "^1.0.0",
    "tslib": "^2.0.3",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemailer": "^6.4.14",
    "puppeteer": "^5.4.1"
  },
  "engines": {
    "node": "12.18.2",
    "yarn": "1.22.4"
  }
}
